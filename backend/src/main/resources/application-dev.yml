# Lokal ishlab chiqish profili. Bot long polling rejimida ishlaydi (§13, 2-bosqich).
bot:
  mode: ${BOT_MODE:polling}
spring:
  jpa:
    show-sql: false
  flyway:
    clean-disabled: true

logging:
  level:
    uz.pochtajp: DEBUG
    org.flywaydb: INFO
