# Prod profili. Bot webhook rejimida ishlaydi (§13, 2-bosqich).
bot:
  mode: ${BOT_MODE:webhook}
spring:
  jpa:
    show-sql: false
  flyway:
    clean-disabled: true

server:
  error:
    whitelabel:
      enabled: false

logging:
  level:
    uz.pochtajp: INFO
